## Debian / núcleo OpenCloud detrás de Nginx + Let's Encrypt (sin Collabora/WOPI)
## En el servidor:
##   cp overrides/opencloud-compose/.env.debian-core-external-proxy.example opencloud-compose/.env
##   chmod 600 opencloud-compose/.env && nano opencloud-compose/.env

COMPOSE_FILE=docker-compose.yml:external-proxy/opencloud.yml
COMPOSE_PROJECT_NAME=opencloud

INSECURE=false
# Production hostname (Google OAuth redirect URIs must match this host)
OC_DOMAIN=cloud.km0digital.com

INITIAL_ADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD

OC_DOCKER_IMAGE=opencloudeu/opencloud-rolling
OC_DOCKER_TAG=7.0.0

LOG_DRIVER=json-file
LOG_LEVEL=info
DEMO_USERS=false

OC_URL=https://cloud.km0digital.com

# Dex OIDC (tras desplegar dex/ y nginx)
OC_OIDC_ISSUER=https://cloud.km0digital.com/dex
WEB_OIDC_CLIENT_ID=opencloud-web
PROXY_OIDC_REWRITE_WELLKNOWN=true
PROXY_OIDC_ACCESS_TOKEN_VERIFY_METHOD=jwt
PROXY_AUTOPROVISION_ACCOUNTS=true
GRAPH_USERNAME_MATCH=none
PROXY_USER_OIDC_CLAIM=email
PROXY_AUTOPROVISION_CLAIM_USERNAME=email
PROXY_USER_CS3_CLAIM=username
PROXY_ROLE_ASSIGNMENT_DRIVER=default
GRAPH_ASSIGN_DEFAULT_USER_ROLE=true
