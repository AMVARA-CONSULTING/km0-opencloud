# Secretos y entorno local
.env
**/.env
!**/.env.example
!**/.env.*.example

# Clon upstream completo en la raíz del repo (mantener en disco; no versionar)
/opencloud-compose/

# Borrador local con secretos históricos
README.red

# Dex / Apple / Google fuera del repo
*.p8
AuthKey_*
dex-data/

# Backups
*.tar.gz
backup*/

# autoagents secrets
autoagents/.env

# autoagents runtime (preflight digests, gh stderr scratch — inside repo, not /tmp)
autoagents/var/loop/
!autoagents/var/.gitkeep

# IDE
.idea/
__pycache__/
*.pyc

# Credenciales en /opt (referenciadas por Dex)
google-client-secret.json
apple-signin-credentials.json
